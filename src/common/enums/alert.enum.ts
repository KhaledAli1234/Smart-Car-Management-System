export enum AlertTypeEnum {
  MAINTENANCE = 'MAINTENANCE',
  FUEL = 'FUEL',
  HEALTH = 'HEALTH',
  BEHAVIOR = 'BEHAVIOR',
  STREAK = 'STREAK',
}

export enum AlertSeverityEnum {
  LOW = 'LOW',
  MEDIUM = 'MEDIUM',
  HIGH = 'HIGH',
}