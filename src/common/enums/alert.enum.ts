export enum AlertTypeEnum {
  MAINTENANCE = 'MAINTENANCE',
  FUEL = 'FUEL',
  HEALTH = 'HEALTH',
  BEHAVIOR = 'BEHAVIOR',
}

export enum AlertSeverityEnum {
  LOW = 'LOW',
  MEDIUM = 'MEDIUM',
  HIGH = 'HIGH',
}